<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Demos.Views.Wordoftheday"  
             xmlns:viewmodel="clr-namespace:Demos.ViewModels"
             xmlns:models="clr-namespace:Demos.Models"
             x:DataType="viewmodel:WordofthedayViewModel"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             Title="Word of the day">

    <ContentPage.BindingContext>
        <viewmodel:WordofthedayViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Behaviors>
        <xct:EventToCommandBehavior EventName="Appearing" Command="{Binding OnAppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Image Source="{Binding WordOfTheDay.ImageUrl}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>